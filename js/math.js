export function add(first, second) {
    return first + second
}

export const arrowAddFull = (first, second) => {
    return first + second
}

export const arrowAddShort = (first, second) => first + second
